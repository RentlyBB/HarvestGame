using System.Collections;
using UnityEngine;

public abstract class Plantable : MonoBehaviour {

    [SerializeField] protected CropSO cropData;
    [SerializeField] protected int currentPhase;

    //Logic for plantable objects
}
